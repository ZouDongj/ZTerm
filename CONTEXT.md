# ZTerm 交互与光标术语

这里只定义本项目审查、设计与验收中使用的术语。

## 标签

**标签命中区**：鼠标或键盘用于选择、关闭、重连、拖动一个标签的交互区域。它与完整名称提示的展示区域不是同一个概念。

**悬停揭示**：指针或键盘焦点进入后展示附加名称、动作或状态；不等同于扩大标签及推动相邻标签。

## 终端光标

**协议光标**：终端协议维护的插入位置、可见性和形状。下游应用可能将其隐藏并停放在与输入框无关的位置。
_Avoid_: 硬件光标（此处不表示操作系统鼠标指针或物理硬件）

**软件绘制光标**：TUI 通过字符或单元格样式绘制的编辑位置标记。它可能覆盖空格，也可能覆盖已有字符。

**光标接管**：ZTerm 对已确认的软件绘制光标提供唯一的平滑视觉表示，同时保持应用内容及协议状态正确。不是让两个光标同时显示。

**原始显示**：应用输出按原有终端语义解析和展示、未额外接管软件绘制光标的结果。它可包含非平滑的软件光标，也可合法地没有光标。

**原子呈现单元**：可可靠确定开始、结束和最终显示意图的一组 TUI 更新。它不等同于一次 IPC 消息或一次任意网络分块。
